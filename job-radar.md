# Job Radar — 프론트엔드 채용 레이더 (프로젝트 재개 프롬프트)

새 채팅방에 이 문서를 그대로 붙여넣으면, 별도 설명 없이 작업을 이어갈 수 있다.
대상자 호칭은 창석. 조언·평가는 위로 없이 솔직·전략적으로.

## 0. 한 줄 정의

송창석(프론트엔드 개발자)의 채용공고 자동수집·분석 대시보드.
매일 자동으로 공고를 모아 → 적합도 채점 + 마감/전형/코딩테스트 + 회사분석 브리프 + 전략 티어까지 자동 생성 → `src/data/jobs.json`에 누적·중복제거 → git push → GitHub Pages `/job-radar`에서 PC·폰 어디서나 열람.

## 1. 사용자 프로필 (채점 기준의 근거)

총 경력 2019.03~ 만 7년+ 프론트엔드 개발자 (웹 퍼블리셔 2년 포함, React·Next.js 기반 FE 전환 후 약 5년).

- 어바웃피싱 (2025.01~2025.10 정규직, 2026.02~ 프리랜서): 낚시 포인트 맵 — 지도(네이버맵 클라이언트 클러스터링)·웹뷰 하이브리드(브릿지 인증 동기화, safe-area, 웹/웹뷰 분기 로그인·약관 동의), 북마크 시스템 신규 설계, 댓글·무한스크롤, 중고장터(이니시스·팝빌·더치트 연동), 샵 장바구니·쿠폰, 백오피스. 프리랜서 기간에 **예약 파트너 관리 웹앱 신규 구축** — 파트너사용 B2B 운영 도구(예약 캘린더·현황, 선박/낚시터 상품 CRUD, 일자별 재고, 직접 예약 생성, 일괄 수정), React Query 기반 도메인별 API 구조 설계
- 메디쿼터스 (2025.12~2026.02): NUGU 일본 패션 이커머스 운영 대응
- 스마일벤처스 (2022.08~2024.01): 캐치패션(MAU 30만 럭셔리 커머스) 리뉴얼 — 장바구니·주문, 관부가세·프로모션·포인트 할인 계산, PG 연동(토스페이·카카오페이)
- 데이터유니버스 (2021.06~2022.08): Vue→React 마이그레이션, 지도 API 전환
- 헥토이노베이션 (2019.03~2021.04): SKT PASS 앱 웹뷰 퍼블리싱

검증 도메인: 이커머스(결제·할인·주문 플로우) · 지도 기반 서비스 · 웹뷰 하이브리드 앱 · SEO · 예약/재고 운영 어드민(B2B 파트너 도구).
임팩트 증거: PG 연동·결제 로직 안정화로 **CVR +14.27%**, SEO 구조 구축으로 **CTR 11.3%·클릭 +41.8%·활성 사용자 +61.1%**.
기술: React, Next.js, TypeScript, Vue/Nuxt, Tailwind, Redux/Zustand/React Query/SWR, Storybook, GraphQL.
라이브 이력서·포트폴리오: https://songcs0329.github.io/songcs-resume/

## 2. ★ 채점·브리프 핵심 프레임 (절대 어기지 말 것)

- **주축(spike) = 복잡한 도메인의 FE 구조 설계.** 두 축이 본진:
  1. 이커머스 결제·할인·주문 플로우 — 관부가세·프로모션·포인트 계산, PG 연동까지 직접 구현 (CVR +14.27% 증거)
  2. 지도·웹뷰 하이브리드 — 클라이언트 마커 클러스터링, 웹/웹뷰 분기 인증 플로우, 네이티브 브릿지 상태 동기화
- **가산점 (주축 아님):** SEO·데이터 기반 개선(서치콘솔 분석→전략 수립), 어드민/백오피스·B2B 파트너 운영 도구 구축(예약·재고 관리 포함), 외부 API 연동 경험(PG·본인인증·계좌인증·사기조회)
- **하향 기준:** 네이티브 앱(RN·Flutter·Swift/Kotlin) 주축 직무, 단순 퍼블리싱·마크업 직무, 3D/WebGL·게임 등 미경험 특화 영역
- **직책 비중 감점 (IC 우선):** 조직 리딩 성격이 강할수록 점수를 낮춘다. 팀장·파트장·실장·그룹장·EM 등 순수 관리 직책 **-20점 + fit_tier 한 단계 하향**, 테크 리드·리드 개발자·챕터 리드 등 리드 겸 IC **-10점**, 채용·평가·1on1 같은 인원 관리가 주요 업무에 명시되면 **추가 -5점**. 시니어 표기만 있고 조직 관리가 없으면 조정 없음. 제외가 아니라 **점수만 낮춰 하위 노출**, 감점 사유는 `fit_reasons`에 명시.
- **풀스택 공고 처리:** "풀스택" 단어만으로 제외하지 않는다. 주요 업무·자격요건으로 FE:BE 비중을 판정 — FE **70% 이상**(BE는 BFF·API 연동 수준)이면 수집·감점 없음(`domains`에 "풀스택" 추가), FE **50~70%**면 수집하되 **-10점**, FE **50% 미만**이거나 BE 언어(Java·Kotlin·Go·Python) 실무 경력을 자격요건으로 요구하면 **제외**. 판정 근거는 `brief.verdict`에 한 줄 기록.
- **연차 필터:** `exp_min_years >= 10` 공고는 수집·표시 모두 제외. 3년 미만 주니어 전용 공고도 제외. (총 경력 7년 기준이지만 8~9년 요구 공고는 지원 가능 범위로 보고 통과시킨다. 공고가 "FE 경력 N년"을 명시하면 FE 5년 기준으로 판단)
- **중복 공고 제거:** 같은 공고가 원티드·점핏·자사홈에 동시에 올라온다. url이 달라도 **company + title이 같으면 한 건만** 남긴다(정규화 비교: 괄호·「(경력)」「채용」「모집」 등 수식어 제거, 소문자화, 회사명의 "(주)·주식회사·Inc·Corp" 제거). 우선순위 **자사홈 > wanted > jumpit**. 회사는 같은데 직무 범위가 달라 보이면 버리지 않고 둘 다 남긴다.
- **수집 기간 필터:** 게시일이 오늘 기준 3개월 이내인 공고만 수집. 게시일을 확인할 수 없는 공고(상시채용 등)는 포함하되 `first_seen`(최초 수집일)을 기록하고, first_seen 기준 3개월이 지나면 jobs.json에서 삭제.
- **이상적 타깃:** 커머스·버티컬·O2O 서비스의 웹 프론트엔드 — 결제/주문 플로우나 지도·웹뷰 접점이 있는 포지션
- **금지 표현:** "정확히 일치", "완벽한 핏" 류 과장 금지. → "결제·커머스 도메인 깊이가 본인 경험과 잘 맞음", "SEO·데이터 개선은 가산점" 톤으로.

## 3. 3티어 전략 (티어 ≠ 적합도, 투자 단계·투자액 기준)

전 구간 동시 지원하되, 티어마다 다른 무기를 든다. 무차별 지원 아님.

| 티어 | 대상 | 무기 버전 | 기대 |
|------|------|-----------|------|
| 🚀 문샷 | 시리즈B 이상 **그리고** 누적 투자 100억+ (상장사·대기업 계열·빅테크는 자동 문샷) | 성과 수치 전면(CVR +14.27%, CTR 11.3%, 활성 사용자 +61%) + 대규모 서비스 경험(MAU 30만 커머스, SKT PASS) | 낮은 확률·면접 데이터 축적용. "버리는 곳" 아님 |
| 🎯 메인 | 시리즈A~B **그리고** 누적 투자 10억+ 이커머스·버티컬·O2O FE | 이커머스 결제 + 지도·웹뷰 하이브리드 풀스토리(주축 2개 모두) | 실제 합격 구간(현실적 오퍼) |
| 🚪 문열기 | seed·프리A 초기 스타트업 FE(리드·1인 FE 포함) | 0→1 폭넓음 — 퍼블리셔→FE 성장 서사, 혼자서 SEO·어드민·인증·결제·배포까지 전 영역 | 폭넓은 커버리지가 가장 강하게 먹히는 곳 |

**티어 판정 규칙:**
- 투자액 = **누적 투자 유치액** 기준 (더브이씨·혁신의숲·뉴스 등으로 확인).
- 상장사·대기업 계열사(카카오·토스·CJ 등)·빅테크는 시리즈 무관 자동 문샷.
- 시리즈B 경계는 투자액으로 판정: 누적 100억+ → 문샷, 10억~100억 → 메인.
- 투자정보를 확인할 수 없는 초기 스타트업은 **문열기로 보수 배정**하고 brief에 "투자정보 미확인" 표기.
- 판정 근거(시리즈·누적 투자액)는 `scale` 필드에 기록 (예: "시리즈B · 누적 290억").

수집기가 신규 공고에 티어를 자동 배정. 대시보드에 티어 배지·필터·"무기:…" 줄 표시.

## 4. 대시보드 기능 명세

- **★ URL 실검증 (절대 규칙):** **HTTP 200 + 공고 본문 확인**을 통과한 URL만 jobs.json에 들어간다. 검증은 수집기(Actions)가 하고, 큐레이터는 `data/raw-jobs.md`에 적힌 url을 **글자 그대로 복사**한다. 기억·추측으로 URL을 조합하는 것 금지.
- **자동수집(Actions):** 평일 08:30 KST `job-radar-collect.yml`. 플랫폼을 먼저 돌고 자사 페이지로 보완한다.
  1. **채용 플랫폼 목록 API (주력)** — 원티드·점핏. **새 회사 발굴은 사실상 여기서만 일어난다.** 경력 범위를 API가 숫자로 주므로 연차 필터를 수집 단계에서 건다(하한 10년 이상·상한 3년 미만 제외 — `job_radar_collect.py`의 `MAX_YEARS`). 점핏은 `closedAt`으로 마감일까지 따라온다.
  2. **자사 채용페이지** — 플랫폼에 공고를 안 올리는 회사 보완(카카오페이·무신사·당근). 루트 HTML에서 상세 링크를 추출해 각각 검증. 목록을 클라이언트에서 그리는 사이트(SPA)는 링크가 안 나와 제외.
  3. **수동 추가** — 창석이 "이 공고 추가해줘 [URL]"로 요청 시. 플랫폼 무관.
  - 사람인·잡코리아·랠릿·프로그래머스·로켓펀치는 **연결 차단**으로 사용 불가.
  - **1회 적재 상한 20건.** 초과분은 버리지 않고 다음 실행에서 재발견되어 순차 적재된다(큐가 저절로 소진됨).
- **만료 처리(Actions Phase 0):** 매 수집 시 `jobs.json` 전 건의 URL 상태를 확인.
  1. **404(3회 재시도 후에도) → 제거**
  2. 200이어도 본문에 마감 문구가 있으면 → 제거
  3. `000·403·429·5xx`는 **일시 오류로 보고 존치** — 다음 실행에서 재확인
  4. **전체 TTL:** `first_seen` 기준 **90일 초과** → 소스 무관 제거
- **누적·중복제거:** `src/data/jobs.json`에 누적. 공고 id 유지로 추적 끊김 방지. 신규엔 NEW 뱃지. url 중복 + **company+title 중복**(플랫폼 간 동일 공고)을 큐레이터가 함께 거른다 → §2 「중복 공고 제거」.
- **적합도 스코어링:** §2 프레임 기준 0~100점 + 티어 배정 + fit_reasons.
- **마감일 그룹핑:** 임박(D-7) → 2주 이내 → 이번 달 → 이후 → 상시·수시 → 미정 → 마감.
- **전형절차·코딩테스트:** 채용절차 단계 + 코딩테스트/과제/없음 표시·필터. (FE 채용 특화 — 원본의 인적성 필터를 대체)
- **회사분석 브리프(정보 보기):** 회사 현황(투자 단계·성장/리스크) + 강점/약점 매칭 + 서류 통과 확률 + 전략 + 출처. 모든 신규 공고에 자동 생성.
- **필터·검색:** 적합도순 정렬, 적합도 티어, 전략 티어(문샷/메인/문열기), 🛒 결제·커머스 도메인 분리, 신규·키워드 검색.
- **지원 추적:** "지원함" 토글 + 메모. localStorage 저장 — **기기 간 동기화 없음**(정적 방식 트레이드오프).

## 5. 기술 구조

- 호스팅: GitHub Pages — 레포 `songcs0329/songcs-resume`, 주소 https://songcs0329.github.io/songcs-resume/job-radar (React 라우트, 404.html SPA 폴백)
- 스택: Vite + React 19 + react-router 7 + Tailwind 4. Firebase 등 외부 DB 없음.
- 파일 구성:
  - `src/data/jobs.json` — 공고 누적 데이터. `meta.last_collected` + `jobs[]`
  - `src/data/jobRadar.ts` — 타입·티어 무기 문구·버킷 순서 등 상수 (UI 변경 시에만 수정)
  - `src/pages/JobRadar/` — 대시보드 페이지 컴포넌트
  - `scripts/job_radar_collect.py` — 수집·검증기(표준 라이브러리만). `--dry-run`·`--selfcheck`·`--max-new N` 등으로 로컬 테스트 가능
  - `scripts/job_radar_sources.json` — 순회 대상. **회사 추가는 이 파일만 고치면 된다.** 추가 전 루트 HTML에서 링크가 실제로 뽑히는지 확인할 것
  - `data/raw-jobs.md` — 수집기 → 큐레이터 인계 큐. **처리 대기분만** 남긴다
  - `data/raw-jobs-archive.md` — 처리 완료분 보관. 중복 수집을 막는 url 원장이고, **루틴은 읽지 않고 덧붙이기만 한다** (읽으면 매 실행 수만 토큰을 태운다)
- **수집기 필드 규칙 (jobs.json 작성 시 반드시 지킬 것):**
  - `first_seen` — 최초 수집일(YYYY-MM-DD), 모든 공고 필수. `posted_date` — 게시일 확인 시 기록.
  - `scale` — 티어 판정 근거 필수 기록 (예: "시리즈B · 누적 290억", "상장사 계열", "투자정보 미확인").
  - `domains` — 한글로 기록 (커머스·O2O·핀테크·지도·웹뷰 등). 🛒 결제·커머스 필터가 한글 도메인 기준으로 동작.
  - `deadline` — 날짜형이면 YYYY-MM-DD, 상시·수시는 `deadline: null` + `deadline_type: "rolling"` + `deadline_label`.
  - `level`(요구 연차 표기)·`location`·`source`·`process_confirmed`도 확인되는 대로 채울 것.
- 지원 추적은 localStorage `jobradar_tracking_v1` (코드·데이터와 분리)

## 6. 운영 루틴 (역할 분담)

**네트워크가 필요한 일과 판단이 필요한 일을 분리했다.** 클라우드 루틴 세션은 외부 egress가 막혀 있어 URL 검증을 할 수 없고, 검증 없이 공고를 만들면 죽은 링크가 쌓인다. 검증은 판단이 아니라 상태코드 확인이므로 Actions로 내렸다.

| 단계 | 실행 주체 | 시각 | 하는 일 | 산출물 |
|------|-----------|------|---------|--------|
| 원티드 수집 | **Mac launchd** `job_radar_local.sh` | 평일 08:00 KST | 원티드만 수집·검증 | `data/raw-jobs.md` → main 직접 커밋 |
| 수집·검증 | GitHub Actions `job-radar-collect.yml` | 평일 08:30 KST | 링크 생존 확인, 점핏·자사페이지 순회, 전 건 200 검증 | `data/raw-jobs.md` + `jobs.json` 만료 정리 → main 직접 커밋 |
| 큐레이션 | Claude 루틴 (클라우드) | 평일 09:00 KST | 대기 공고를 읽어 채점·브리프·티어 배정 | `jobs.json` → `claude/` 브랜치 PR |
| 머지 | `auto-merge-jobradar.yml` | PR 생성 시 | squash merge + deploy 트리거 | main |
| 배포 | `deploy.yml` | main 푸시 | 빌드 → Pages (1~2분) | 대시보드 반영 |

- **원티드만 Mac에서 도는 이유:** 원티드 API는 데이터센터 IP에 **403**을 준다(Actions 러너에서 실측). 같은 요청이 주거용 IP인 Mac에서는 200이라 이것만 로컬로 분리했다. Actions는 `--skip-platform wanted`로 건너뛴다.
  - Mac이 꺼져 있으면 그냥 건너뛴다 — 같은 공고가 다음 실행에 다시 잡히므로 손실이 없다.
  - 등록: `launchctl load ~/Library/LaunchAgents/com.songcs.job-radar-local.plist` / 로그: `.job-radar-local.log`
- 수집기는 PR을 만들지 않고 main에 직접 커밋한다 — 기계적 결과라 리뷰할 게 없고, `GITHUB_TOKEN`으로 만든 PR은 다른 워크플로를 트리거하지 못한다.
- 큐레이터는 **웹을 보지 않는다.** `raw-jobs.md`에 없는 공고를 만들어 넣지 않는다.
- **대기 큐는 총량 20건으로 고정.** 넘치는 건은 버려지고 다음 실행에서 다시 발견된다. 큐레이터가 처리해 아카이브로 내려야 새 공고가 들어올 자리가 생긴다.
- **제외한 공고도 아카이브에 넣는다.** jobs.json에 안 들어가므로 아카이브에서 빠지면 수집기가 매일 다시 주워온다.
- **열람:** 배포된 페이지는 항상 열림. 데이터 갱신도 코드(UI) 변경도 모두 "푸시"라는 동일한 경로.

### 로컬에서 수집기 돌려보기

```bash
python3 scripts/job_radar_collect.py --selfcheck              # 네트워크 없이 로직 검사
python3 scripts/job_radar_collect.py --dry-run                # 파일 안 쓰고 결과만
python3 scripts/job_radar_collect.py --skip-phase0            # 링크 확인 건너뛰고 수집만
python3 scripts/job_radar_collect.py --only wanted            # 원티드만 (로컬 수집과 동일)
python3 scripts/job_radar_collect.py --skip-platform wanted   # 원티드 제외 (Actions와 동일)
python3 scripts/job_radar_collect.py --max-pending 5          # 큐 총량 상한 조정
bash scripts/job_radar_local.sh                               # launchd가 도는 것과 동일 경로
```

## 7. 알려진 제약·주의

- **루틴 세션은 외부 웹에 접근할 수 없다.** curl·WebFetch 모두 403(정책 차단) — 특정 사이트가 아니라 egress 자체가 막혀 있다. 그래서 수집·검증을 Actions로 분리했다. 루틴 프롬프트에 웹 접근을 전제한 지시를 다시 넣지 말 것.
  - 2026-08-08 이전 수집분은 이 제약 때문에 웹을 못 보는 채로 URL이 생성되어 **죽은 링크 21건**이 섞여 있었음(존재하지 않는 서브도메인까지 조합됨) → 정리 완료.
- **원티드·점핏 API는 비공식이다.** 공개 문서가 없어 응답 형태가 예고 없이 바뀔 수 있다. 바뀌면 해당 플랫폼만 건너뛰고 나머지 수집은 계속되도록 예외 처리돼 있다. 호출 간 1초 간격, 하루 1회.
- **원티드는 데이터센터 IP를 403으로 막는다.** GitHub Actions 러너에서 재현됨 — 그래서 Mac launchd로 분리했다. Actions 워크플로에 원티드를 다시 넣지 말 것. 점핏은 러너에서도 정상.
- **회사가 제목과 메타데이터를 다르게 적는 경우가 있다.** 점핏에서 제목은 "9년~"인데 API `minCareer`는 6인 공고가 실제로 있었다. 연차 필터는 API 값을 쓰므로 이런 건 통과한다 — 최종 판단은 본문을 읽는 큐레이터 몫.
- **기존 공고 중 일부는 개별 공고가 아니라 채용 목록 루트 URL이다** (`ably.team/recruit`, `career.kakaostyle.com/jobs` 등 9건). 200이라 Phase 0에서 안 걸러진다. 열어도 해당 공고가 없으니 별도 정리 필요.
- 스케줄 태스크는 활성 1개만 유지(중복 사용량 방지). 데이터 반영에 배포 시간 1~2분 소요.
- 데이터 갱신 커밋이 레포 히스토리에 쌓임. 부담되면 추후 Firebase(Firestore) 전환 — 데이터 로딩 레이어만 교체하면 됨.
- 지원함·메모는 기기별 localStorage라 PC·폰 간 동기화 안 됨.

## 8. 지금 상태 / 다음 할 일

현재 공고 목록은 `src/data/jobs.json` (`meta.last_collected`, `meta.total`, `jobs[]`) 참조. 이 문서에 별도 기재하지 않음 — 이중 관리 방지.

- **수집 로직·소스**: `scripts/job_radar_collect.py` + `scripts/job_radar_sources.json` (레포 안, 단일 관리)
- **채점·브리프 기준**: `~/.claude/scheduled-tasks/job-radar-curation/SKILL.md` (= 루틴 프롬프트와 동일 내용)

## 9. 순회 대상 회사 추가하기

`scripts/job_radar_sources.json`의 `sources`에 한 줄 추가하면 된다. 추가 전 링크가 실제로 뽑히는지 확인:

```bash
curl -s -L -A 'Mozilla/5.0' "https://회사.career.greetinghr.com/" | grep -o -E '/o/[0-9]+' | sort -u
```

0건이면 목록을 클라이언트에서 그리는 사이트(SPA)라 순회 불가 — 추가하지 말 것. greetinghr도 회사에 따라 SSR/SPA가 갈린다.

플랫폼에 공고를 올리는 회사는 원티드·점핏에서 자동으로 잡히므로 **자사 페이지에 따로 넣을 필요가 없다.** 여기 넣는 건 플랫폼에 안 올리는 회사(당근 등)뿐.
